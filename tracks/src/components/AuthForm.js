import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Form = () => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default Form

const styles = StyleSheet.create({})
